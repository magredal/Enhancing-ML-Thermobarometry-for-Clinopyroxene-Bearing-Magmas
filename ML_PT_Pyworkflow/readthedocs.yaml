version: 2

build:
  os: macOS-14.5
  tools:
    python: "3.11.5"
  jobs:
    pre_build:
      - pip install --upgrade pip

sphinx:
  configuration: docs/source/conf.py

python:
  install:
    - requirements: requirements.txt
